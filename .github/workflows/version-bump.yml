# Version bump is now handled atomically inside publish.yml
# This file is kept as a placeholder to avoid broken references.
name: Version Bump (merged)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Version bump is now part of the Release workflow (publish.yml)"
